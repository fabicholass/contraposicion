name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Deploy to GitHub Pages
        env:
          PERSONAL_ACCESS_TOKEN: ${{ secrets.ACTIONS_DEPLOY_KEY }}
        run: |
          git remote set-url origin https://x-access-token:${PERSONAL_ACCESS_TOKEN}@github.com/tu_usuario/tu_repositorio.git
          git checkout -b gh-pages || git checkout gh-pages
          mkdir -p ./docs
          cp -R * ./docs
          echo "donalgo" > ./docs/index.html
          echo "fabi" > ./docs/fabi.html
          echo "elfb34" > ./docs/elfb34.html
          echo "lux" > ./docs/lux.html
          echo "snas" > ./docs/snas.html
          echo "cosmos" > ./docs/cosmos.html
          git add .
          git commit -m "Deploy to gh-pages with subdomains"
          git push origin gh-pages
